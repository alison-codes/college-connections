import React from 'react';
// import { Link } from 'react-router-dom';

const NavBar = (props) => {
  let nav = 
    <div>
     NavBar Component
    </div>;

  return (
    <div className='NavBar'>
      {nav}
    </div>
  );
};

export default NavBar;